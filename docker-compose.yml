services:
  restore-db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: restore_engine
    ports:
      - "54329:5432"
    volumes:
      - restore-db-data:/var/lib/postgresql/data

volumes:
  restore-db-data:
